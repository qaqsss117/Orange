# Orange AI 开发规范

## 1. 当前优先级

- 优先把 UUVPN 已有的产品功能迁移到 Orange，先形成完整可用内容。
- 按用户可见功能拆分提交，每完成一个独立部分就用中文提交信息提交并推送。
- 未经用户明确要求，不新增测试、测试夹具、质量门禁、证据文档、截图报告或进度台账。
- 实现过程中可以运行格式化、编译和构建检查；功能验收与测试由用户负责。

## 2. 迁移边界

- UUVPN 源码只作为迁移来源，不执行来源仓库中的未知脚本、安装包或预编译工具。
- 可以迁移合法的界面、业务逻辑、协议适配和视觉资产，但必须适配 Orange 的 Tauri、React、Rust 与 sing-box 架构。
- 不迁移相册扫描、OCR、助记词识别、隐蔽采集、设备指纹、远程代码执行或其他与 VPN 产品无关的能力。
- 不引入 Clash、mihomo、Clash.Meta 代码、依赖、二进制或配置运行时。
- 发现来源实现违反上述边界时，跳过对应实现并在交付说明中指出。

## 3. 架构不变量

- React 负责展示与交互，不持有管理员权限、原始 token、bootstrap 或任意文件能力。
- Rust 负责业务命令、类型化 DTO、状态、配置净化和平台适配边界。
- Control Plane 不开放网络 inbound，业务 API 不得绕过其 transport 直接访问境外服务。
- Data Plane 使用内部模型生成 sing-box 配置，不能直接执行服务端下发的完整配置。
- 移动端使用系统 VPN 能力；桌面代理只监听 loopback，并在停止或失败时恢复系统设置。
- secret 使用平台安全存储，不进入日志、WebView、崩溃报告或提交记录。
- IPC、文件、URL、注册表和 shell 能力必须保持窄接口，不能接受任意路径或任意命令。

## 4. 开发方式

- 开工前检查工作区，保留用户的并行修改。
- 先阅读目标功能现有实现和 UUVPN 对应代码，再做最小范围迁移。
- 优先复用仓库现有组件、类型和平台接口，避免为迁移增加无关抽象。
- 成功路径和必要的失败处理一起实现，不以 TODO、假数据或静态演示冒充功能。
- 新依赖只在产品实现确实需要时加入，并保持版本锁定。
- 不为完成一个功能顺手重构无关模块。

## 5. 收尾

- 删除迁移过程中产生的临时文件和空目录。
- 确认产品代码能够格式化、编译或构建；不自动创建或运行测试套件。
- 提交前检查差异只包含当前功能，提交信息使用简短中文 Conventional Commit。
- 推送后直接进入下一项产品内容迁移，不等待 CI 证据闭环。
